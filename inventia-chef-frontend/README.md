# InventIA Chef — Frontend

Interfaz en React + Vite. La API se consume en rutas `/api/*` (proxy en desarrollo hacia el backend).

## Comandos

```bash
npm install
npm run dev      # desarrollo
npm run build    # producción
npm run e2e      # tests Playwright
```

Documentación general del TFC: ver `../README.md`.
